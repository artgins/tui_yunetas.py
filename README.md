# TUI for yunetas


## Install PDM
Ensure PDM is installed. If not, install it using pip:

```shell
  pip install pdm
```

## Build the package

```shell
  pdm build
```

## Publish the package

```shell
  pdm publish
```
